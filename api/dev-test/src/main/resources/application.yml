spring:
  security:
    jwt:
      secret-key: "ABCSECRET123"
      expiration-time: 159871200
      issuer: qewqewqeq
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  application:
    name: ${APP_NAME}
server:
  port: ${APP_PORT}